{
  "name": "@pind/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Generated database types, fixed copy, constants and design tokens shared by the Worker and the app",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "gen:types": "supabase gen types typescript --linked --schema public > src/database.types.ts"
  }
}
