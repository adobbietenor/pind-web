// A5 — Home: This Week's Crowds (empty state first; A6 sparse and A7 populated
// arrive in M3.2). Scaffold only (M2.0): no product logic yet.
import { Screen } from "@/components/Screen";

export default function Crowds() {
  return <Screen title="Crowds" />;
}
